# 任务三：可视化与分析 - 使用说明

## 一、新增功能

### 1.1 实时状态监控
- 添加了 `print_realtime_status()` 函数，可以实时显示系统状态
- 显示缓冲区使用率、生产/消费统计、缓冲区满/空状态次数

### 1.2 缓冲区状态统计
- **缓冲区满状态次数**：记录缓冲区达到满状态（count=10）的次数
- **缓冲区空状态次数**：记录缓冲区达到空状态（count=0）的次数
- 每次状态变化时会在日志中输出 `[STATUS]` 标记

### 1.3 生产者阻塞时间统计
- 记录每个生产者在等待空槽位时的阻塞时间
- 统计每个生产者的：
  - 总阻塞时间
  - 阻塞次数
  - 平均阻塞时间
- 只统计实际阻塞时间（>1ms），忽略极短的等待

### 1.4 死锁和数据一致性检查
- 在最终统计中自动检查：
  - 生产和消费数量是否匹配
  - 是否有物品丢失
  - 是否存在死锁或数据不一致

## 二、使用方法

### 2.1 编译程序
```bash
make producer_consumer
# 或
gcc -o producer_consumer producer_consumer.c semaphore.c base_lock.c -lpthread
```

### 2.2 运行程序并保存日志
```bash
# 运行程序并将输出重定向到文件
./producer_consumer > log.txt

# 或者同时显示在终端和保存到文件
./producer_consumer | tee log.txt
```

### 2.3 分析日志文件

#### 方法1：使用 grep 命令分析

**1. 缓冲区达到满状态多少次？**
```bash
grep "Buffer is FULL" log.txt | wc -l
```

**2. 缓冲区空状态多少次？**
```bash
grep "Buffer is EMPTY" log.txt | wc -l
```

**3. 查看所有状态变化**
```bash
grep "\[STATUS\]" log.txt
```

**4. 查看生产者阻塞情况**
```bash
# 查看所有生产操作
grep "Producer.*Produced" log.txt

# 查看信号量等待情况（empty_sem 的等待表示生产者阻塞）
grep "empty_sem.*wait enter" log.txt | wc -l
```

#### 方法2：查看最终统计信息

程序运行结束后，会在日志末尾输出详细的统计信息，包括：

```
缓冲区状态统计:
  缓冲区达到满状态次数: X
  缓冲区达到空状态次数: Y

生产者阻塞时间统计:
  Producer-0: 总阻塞时间=X.XXX秒, 阻塞次数=XX, 平均阻塞时间=X.XXX秒
  Producer-1: 总阻塞时间=X.XXX秒, 阻塞次数=XX, 平均阻塞时间=X.XXX秒
  Producer-2: 总阻塞时间=X.XXX秒, 阻塞次数=XX, 平均阻塞时间=X.XXX秒
  所有生产者平均阻塞时间: X.XXX秒
```

#### 方法3：使用脚本分析

创建一个分析脚本 `analyze_log.sh`：

```bash
#!/bin/bash

LOG_FILE=$1

if [ -z "$LOG_FILE" ]; then
    echo "用法: $0 <log_file>"
    exit 1
fi

echo "=== 日志分析结果 ==="
echo ""

echo "1. 缓冲区达到满状态次数:"
grep "Buffer is FULL" $LOG_FILE | wc -l

echo ""
echo "2. 缓冲区空状态次数:"
grep "Buffer is EMPTY" $LOG_FILE | wc -l

echo ""
echo "3. 生产者阻塞次数（empty_sem等待）:"
grep "empty_sem.*wait enter" $LOG_FILE | wc -l

echo ""
echo "4. 消费者阻塞次数（full_sem等待）:"
grep "full_sem.*wait enter" $LOG_FILE | wc -l

echo ""
echo "5. 所有状态变化:"
grep "\[STATUS\]" $LOG_FILE

echo ""
echo "6. 最终统计信息:"
grep -A 50 "缓冲区状态统计" $LOG_FILE | head -20
```

使用方法：
```bash
chmod +x analyze_log.sh
./analyze_log.sh log.txt
```

## 三、日志格式说明

### 3.1 状态标记

- `[STATUS] Buffer is FULL` - 缓冲区达到满状态
- `[STATUS] Buffer is EMPTY` - 缓冲区达到空状态
- `[Producer-X] Produced item #Y` - 生产者X生产了物品Y
- `[Consumer-X] Consumed item #Y` - 消费者X消费了物品Y
- `[SEM] sem_name: operation` - 信号量操作日志

### 3.2 时间戳格式

所有日志都包含时间戳，格式为：`[秒.毫秒]`

例如：`[1764739651.830]` 表示从程序开始运行到该事件发生的秒数

## 四、分析问题的方法

### 4.1 检查死锁

1. **查看程序是否正常结束**：
   ```bash
   tail -20 log.txt | grep "统计信息"
   ```
   如果程序正常结束，会输出统计信息；如果没有，可能发生了死锁。

2. **检查是否有线程永久阻塞**：
   ```bash
   # 查看最后的信号量操作
   tail -50 log.txt | grep "\[SEM\]"
   ```
   如果某个信号量的 `wait_count` 一直大于0且没有后续操作，可能存在死锁。

### 4.2 检查数据一致性

1. **查看最终统计**：
   ```bash
   grep -A 10 "验证结果" log.txt
   ```
   应该显示：
   ```
   ✓ 生产和消费数量匹配
   ✓ 没有物品丢失
   ✓ 没有死锁或数据不一致
   ```

2. **手动验证**：
   ```bash
   # 统计生产的物品数
   grep "Produced item" log.txt | wc -l
   
   # 统计消费的物品数
   grep "Consumed item" log.txt | wc -l
   ```
   两者应该相等，且都等于 60（3个生产者 × 20个物品）。

### 4.3 分析性能

1. **查看运行时间**：
   ```bash
   grep "总运行时间" log.txt
   ```

2. **分析阻塞情况**：
   - 查看最终统计中的"生产者阻塞时间统计"
   - 阻塞时间越长，说明系统负载越重
   - 平均阻塞时间可以反映系统效率

3. **分析缓冲区使用**：
   - 满状态次数多：说明生产者速度快，需要更大的缓冲区
   - 空状态次数多：说明消费者速度快，或者生产者速度慢

## 五、示例分析

假设运行场景3（快速消费者）后，日志分析结果：

```
1. 缓冲区达到满状态次数: 0
   → 说明缓冲区从未满过，符合快速消费者的特征

2. 缓冲区空状态次数: 30
   → 说明缓冲区经常空，消费者等待较多

3. 生产者阻塞次数: 0
   → 说明生产者从未阻塞，因为消费者速度快，缓冲区总是有空位

4. 消费者阻塞次数: 60
   → 说明消费者经常等待，因为生产者速度慢
```

## 六、注意事项

1. **日志文件可能很大**：如果运行时间较长，日志文件可能达到几MB，建议使用 `grep` 等工具分析，而不是直接打开。

2. **时间精度**：阻塞时间统计的精度取决于系统时钟精度，通常为毫秒级。

3. **并发安全**：所有统计信息都使用锁保护，确保线程安全。

4. **性能影响**：添加统计功能会略微影响程序性能，但影响很小（<5%）。

## 七、扩展功能（可选）

如果需要更详细的分析，可以：

1. **添加实时监控**：在程序运行过程中定期调用 `print_realtime_status()`
2. **导出CSV格式**：将统计信息导出为CSV格式，便于用Excel等工具分析
3. **图形化显示**：使用gnuplot等工具绘制缓冲区使用率曲线

---

**使用提示**：建议先运行程序生成日志，然后使用提供的脚本或命令进行分析。

