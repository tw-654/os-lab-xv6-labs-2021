# 实验五：程序内存地址分配分析报告

## 实验信息

- **实验名称**：程序内存地址分配分析
- **实验日期**：_____________
- **学生姓名**：_____________
- **学号**：_____________

---

## 一、实验环境

- **操作系统**：_____________
- **编译器**：_____________
- **编译选项**：`gcc -Wall -Wextra -std=c99 -g -fno-omit-frame-pointer`
- **分析工具**：objdump, readelf, gdb, nm

---

## 二、内存布局分析

### 2.1 各内存区域的地址范围

| 内存区域 | 地址范围 | 大小 | 特点 |
|---------|---------|------|------|
| 代码区 (Text) | | | 只读、可执行 |
| 数据区 (Data) | | | 可读写、已初始化全局变量 |
| BSS 区 | | | 可读写、未初始化全局变量 |
| 堆区 (Heap) | | | 动态分配、向上增长 |
| 栈区 (Stack) | | | 自动分配、向下增长 |

### 2.2 实际观察到的地址

#### 代码区地址

```
main() 函数地址:            _____________
add() 函数地址:             _____________
increment_point() 函数地址: _____________
print_memory_info() 地址:   _____________
```

**观察结果**：
- 代码区地址范围：_____________
- 代码区特点：_____________
- 代码区大小：_____________ 字节

#### 数据区地址

```
global_var 地址:           _____________
static_global_var 地址:     _____________
```

**观察结果**：
- 数据区地址范围：_____________
- 数据区特点：_____________
- 数据区大小：_____________ 字节

#### 栈区地址

```
main() 中的 stack_var:     _____________
main() 中的 sum:           _____________
main() 中的 heap_point:    _____________
add() 中的 result:         _____________
add() 中的参数 a:          _____________
add() 中的参数 b:          _____________
```

**观察结果**：
- 栈区地址范围：_____________
- 栈增长方向：_____________
- 栈帧大小（main）: _____________ 字节
- 栈帧大小（add）: _____________ 字节

#### 堆区地址

```
malloc 分配的 heap_point:  _____________
heap_point 指向的地址:     _____________
```

**观察结果**：
- 堆区地址范围：_____________
- 堆增长方向：_____________
- 堆内存大小：_____________ 字节

### 2.3 地址分布规律分析

**分析要点**：
1. 地址大小的关系：代码区 < 数据区 < 堆区 < 栈区
2. 为什么是这样的分布：_____________
3. 地址空间的特点：_____________

---

## 三、函数调用机制分析

### 3.1 add() 函数分析

#### 函数原型
```c
int add(int a, int b) {
    int result;
    result = a + b;
    return result;
}
```

#### 反汇编代码

```assembly
<add>:
    (在此处粘贴 objdump 输出的反汇编代码)
```

#### 分析结果

**参数传递方式**：
- 参数 `a` 通过 _____________ 传递（寄存器/栈）
- 参数 `b` 通过 _____________ 传递（寄存器/栈）
- 参数存储位置：_____________
- 参数地址：`&a = _____________`, `&b = _____________`

**局部变量**：
- 局部变量 `result` 存储在：_____________
- 地址：`&result = _____________`

**返回值传递**：
- 返回值通过 _____________ 传递（寄存器/栈）
- 寄存器名称：_____________

**栈帧结构**：
```
高地址
  [________________]  ← rbp
  [________________]  ← 返回地址
  [________________]  ← 参数/寄存器保存
  [________________]  ← 局部变量
  [________________]  ← rsp
低地址
```

**栈帧大小**：_____________ 字节

### 3.2 increment_point() 函数分析

#### 函数原型
```c
void increment_point(struct Point *p) {
    if (p == NULL) {
        printf("Error: Null pointer received!\n");
        return;
    }
    p->x += 1;
    p->y += 1;
}
```

#### 反汇编代码

```assembly
<increment_point>:
    (在此处粘贴 objdump 输出的反汇编代码)
```

#### 分析结果

**参数传递方式**：
- 指针参数 `p` 通过 _____________ 传递
- 参数 `p` 存储位置：_____________
- `p` 指向的地址：_____________（堆内存地址）

**指针操作**：
- 如何检查 NULL：_____________
- 如何访问 `p->x`：_____________
- 如何访问 `p->y`：_____________

**返回值**：
- 函数无返回值（void）

**栈帧大小**：_____________ 字节

### 3.3 栈帧对比分析

| 函数 | 栈帧大小 | 参数数量 | 局部变量数量 | 特点 |
|-----|---------|---------|------------|------|
| main() | | | | |
| add() | | | | |
| increment_point() | | | | |

**分析**：_____________

---

## 四、参数传递方式对比

### 4.1 值传递 (Pass by Value)

**示例**：`add(int a, int b)`

- 参数传递机制：_____________
- 内存使用：_____________
- 函数内修改的影响：_____________
- 优点：_____________
- 缺点：_____________

### 4.2 指针传递 (Pass by Reference)

**示例**：`increment_point(struct Point *p)`

- 参数传递机制：_____________
- 内存使用：_____________
- 函数内修改的影响：_____________
- 优点：_____________
- 缺点：_____________

### 4.3 对比总结

| 特性 | 值传递 | 指针传递 |
|-----|-------|---------|
| 参数大小 | | |
| 内存开销 | | |
| 修改影响 | | |
| 适用场景 | | |

---

## 五、堆内存管理分析

### 5.1 malloc 分配分析

```c
struct Point *heap_point = (struct Point *)malloc(sizeof(struct Point));
```

**观察结果**：
- `heap_point` 指针的地址（栈上）：_____________
- `heap_point` 指向的地址（堆上）：_____________
- 分配的内存大小：_____________ 字节
- 堆地址的特点：_____________

### 5.2 free 释放分析

```c
free(heap_point);
heap_point = NULL;
```

**观察结果**：
- `free()` 后的地址变化：_____________
- 设置为 NULL 的原因：_____________

### 5.3 堆与栈的对比

| 特性 | 堆 (Heap) | 栈 (Stack) |
|-----|----------|-----------|
| 分配方式 | | |
| 释放方式 | | |
| 大小限制 | | |
| 访问速度 | | |
| 碎片问题 | | |
| 增长方向 | | |

---

## 六、程序节（Section）分析

### 6.1 使用 objdump 查看节信息

```bash
objdump -h func_demo
```

**主要节信息**：

| 节名 | 大小 | 虚拟地址 | 特点 |
|-----|------|---------|------|
| .text | | | 代码段 |
| .data | | | 数据段 |
| .bss | | | BSS段 |
| .rodata | | | 只读数据 |
| .symtab | | | 符号表 |

### 6.2 使用 readelf 查看程序头

```bash
readelf -l func_demo
```

**主要段信息**：

| 段类型 | 虚拟地址 | 物理地址 | 大小 | 权限 |
|-------|---------|---------|------|------|
| LOAD (代码段) | | | | R E |
| LOAD (数据段) | | | | RW |

---

## 七、符号表分析

### 7.1 使用 nm 查看符号

```bash
nm func_demo
```

**主要符号类型**：

| 符号 | 类型 | 地址 | 说明 |
|-----|------|------|------|
| main | T | | 代码段中的函数 |
| add | T | | 代码段中的函数 |
| global_var | D | | 数据段中的全局变量 |

**符号类型说明**：
- `T` / `t`：代码段（Text）
- `D` / `d`：数据段（Data）
- `B` / `b`：BSS段

---

## 八、思考题

### 1. 为什么代码区的地址最小，栈区的地址最大？

**回答**：_____________

### 2. 值传递和指针传递在汇编层面有什么区别？

**回答**：_____________

### 3. 为什么局部变量不能返回其地址给调用者（在某些情况下）？

**回答**：_____________

### 4. 堆和栈在内存管理上有什么不同？

**回答**：_____________

### 5. 函数调用时的开销主要包括哪些？

**回答**：_____________

---

## 九、实验总结

### 9.1 主要收获

1. _____________
2. _____________
3. _____________

### 9.2 遇到的问题及解决方法

1. **问题**：_____________
   **解决方法**：_____________

2. **问题**：_____________
   **解决方法**：_____________

### 9.3 实验心得

_____________

---

## 十、参考资料

1. 《深入理解计算机系统》第 3 章
2. x86-64 System V ABI 规范
3. objdump, readelf, gdb 手册

---

**报告完成日期**：_____________

